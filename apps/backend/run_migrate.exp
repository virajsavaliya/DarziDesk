#!/usr/bin/expect -f
set timeout -1
spawn npx prisma migrate dev --name payment_gateway_prep
expect "We need to reset the PostgreSQL database" {
    send "y\r"
    exp_continue
} "If these variants are still used in the database, this will fail." {
    send "y\r"
    exp_continue
} eof
